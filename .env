# Development environment configuration
# This URL is used when running the app locally with `npm run ios` or `npx expo start`
# For production builds, the URL is configured in eas.json
# Start the mock server with: npm run mock-server

EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
